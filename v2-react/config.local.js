window.MC_V2_CONFIG = {
  // Control Tower uses local/shared JSON sidecars as the active source of truth.
  // Keep this file present because index.html imports it, but do not configure
  // legacy Supabase realtime lanes here unless intentionally rolling back.
};
